# 一、Python中的除法取整

🧮 Python 中的除法取整总结

## 1️⃣ 除法运算符对比

| 操作                 | 示例             | 结果 | 描述                             |
|----------------------|------------------|------|----------------------------------|
| 真除法 `/`           | `5 / 2`          | 2.5  | 返回浮点数                       |
| 地板除（向下取整）`//` | `5 // 2`         | 2    | 向下取整（floor）                |
|                      | `-5 // 2`        | -3   | ⚠ 向负无穷取整，不是 -2          |
| 取模 `%`             | `5 % 2`          | 1    | 满足 `a == b*(a//b) + a%b`       |
| `int()` 截断         | `int(2.9)`       | 2    | 向 0 方向截断                    |
|                      | `int(-2.9)`      | -2   | 与 `//` 行为不同                 |
| `math.floor()`       | `math.floor(2.9)`| 2    | 向下取整                         |
| `math.ceil()`        | `math.ceil(2.1)` | 3    | 向上取整                         |

## 2️⃣ 负数行为对比

| 表达式          | 结果 | 含义                         |
|-----------------|------|------------------------------|
| `-5 // 2`       | -3   | 向负无穷取整                 |
| `int(-5 / 2)`   | -2   | 向 0 截断                    |
| `(-5) % 2`      | 1    | 模运算结果与除数符号一致     |

📌 **记住：**

- `//` 是 floor（向下取整）
- `int()` 是 truncate（向零截断）

## 3️⃣ 如何在算法中选择？

| 应用场景               | 推荐         |
|------------------------|--------------|
| 数学意义上的向下取整   | `//`         |
| 单纯截取整数部分       | `int()`      |
| 需要保持整除关系       | `// + %`     |

## 4️⃣ 示例代码

```python
import math

print(5 / 2)       # 2.5
print(5 // 2)      # 2
print(-5 // 2)     # -3

print(int(5/2))    # 2
print(int(-5/2))   # -2

print(math.floor(-2.1))  # -3
print(math.ceil(-2.1))   # -2